#import "../../lib.typ" as njust

= 系统设计与实现

为了展示模板对正文层级、图表与公式的排版支持，本章给出一个简化的系统实现说明。

== 系统总体结构

系统由环境感知、状态估计、规划决策与底层控制四个部分组成。环境感知模块负责接收并预处理多源传感数据；状态估计模块输出机器人位姿；规划决策模块生成目标轨迹；底层控制模块将轨迹转换为速度指令。

== 关键流程说明

系统执行流程可概括为：接收原始传感数据，更新局部环境信息，估计当前状态，生成或修正目标路径，最后输出运动控制量。上述流程按固定频率循环执行，从而满足实时导航需求。

== 示例公式

设机器人当前位置为 $p_t$，目标点为 $g$，则可用如下代价函数衡量局部规划目标：

$ J = alpha dot d(p_t, g) + beta dot c_t $

其中，$d(p_t, g)$ 表示当前位置到目标点的距离，$c_t$ 表示当前控制代价，$alpha$ 与 $beta$ 为权重系数。

== 示例表格

#njust.table(
  (40%, 20%, 20%, 20%),
  (left, center, center, center),
  [示例系统参数表],
  ([参数名称], [符号], [数值], [单位]),
  3,
  [机器人最大速度], [$v_max$], [1.2], [m/s],
  [规划周期], [$T_p$], [100], [ms],
  [安全距离阈值], [$d_"safe"$], [0.5], [m],
)
