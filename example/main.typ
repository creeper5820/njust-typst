#import "@local/njust-homework:0.1.0": project

#show: project.with(
  title: "RMCS 开发部署流程调研报告",
  college: "自动化学院 机器人工程系",
  major: "智能无人系统技术",
  course-name: "机器人软件工程",
  assignment-name: "开源项目技术调研",
  id: ("922110800011", "922110800012", "922110800013"),
  name: ("张三", "李四", "王五"),
  date: "2026年3月",
  summary: "RMCS 是面向 RoboMaster 场景、基于 ROS2 的控制系统工程。本文围绕其官方文档整理开发与部署主流程，重点分析容器化开发环境、远程部署容器、构建产物同步与服务化运行机制，并讨论该流程在多设备协同调试中的优势与注意事项。",
  key-words: ("RMCS", "ROS2", "Dev Container", "Docker", "远程部署"),
)


= 项目概述

RMCS（RoboMaster Control System）是一个面向机器人竞赛场景的控制系统项目，核心特点是以 ROS2 为基础，并通过容器化手段统一开发与部署环境。项目文档给出了从镜像拉取、工程构建到远程联调的完整工作流，适合团队化协作开发。


= 开发流程梳理

根据仓库说明，开发侧的标准流程可概括为以下步骤：

1. 使用 Docker 镜像拉起 Dev Container，统一编译器与依赖版本；
2. 以 `--recurse-submodules` 方式克隆仓库，保证子模块完整；
3. 在容器中执行 `build-rmcs` 构建 `rmcs_ws`，启用代码补全与调试；
4. 通过 `launch-rmcs` 在本机运行，并用 `set-robot` 切换机器人类型。

该流程将环境差异问题前置消解，减少了“同一代码在不同机器行为不一致”的风险。


= 部署与远程调试机制

RMCS 的部署流程同样采用容器化策略：目标端（如 MiniPC）常驻运行 runtime 容器，开发端通过 `ssh-remote` 建立连接，再以 `sync-remote` 持续同步构建产物。服务由 `service rmcs restart` 管理，日志可通过 `service rmcs attach` 实时查看。

这种“双容器+同步”的方式将开发调试与上线运行解耦：开发容器负责构建与验证，部署容器负责长期稳定运行，便于在比赛现场快速迭代。

= 结论与建议

RMCS 的工程化方案体现了机器人软件系统常见的三项关键能力：环境标准化、远程可运维、服务化生命周期管理。对于课程项目或实验室团队，建议优先复用其容器化流程模板，并结合本地网络与设备权限策略，提前完成 USB 规则、端口占用与远程连接稳定性检查。
